name: Release Nomen

on:
  workflow_dispatch:
  push:
    tags:
      - 'nomen-v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: nomen/package-lock.json

      - run: npm ci
        working-directory: nomen

      - run: npm run pack
        working-directory: nomen
      - run: npm run pack:discovery
        working-directory: nomen

      - run: node scripts/build-pages-site.mjs

      - name: Archive discovery site
        run: tar -C build -czf nomen/build/nomen.app.provable.dev-discovery-${{ github.ref_name || 'manual' }}.tar.gz pages

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nomen-build-assets
          path: |
            nomen/build/*.zip
            nomen/build/*.tar.gz

      - name: Publish release assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            nomen/build/*.zip
            nomen/build/*.tar.gz
